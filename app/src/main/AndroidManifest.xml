<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.WRITE_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SQLite"
        tools:targetApi="31">
        <activity
            android:name=".activities.UserListActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UpdateUserActivity"
            android:exported="false" />
        <activity
            android:name=".activities.NewUserActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserManagementActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserDataActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:readPermission="es.ua.eps.READ_PERMISSION"
            android:writePermission="es.ua.eps.WRITE_PERMISSION"
            android:permission="es.ua.eps.PERMISSION"
            android:grantUriPermissions="true"
            android:authorities="es.ua.eps.usersprovider"
            android:name=".providers.UsersContentProvider" />
    </application>

</manifest>